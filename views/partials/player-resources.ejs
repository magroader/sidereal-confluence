<h1>Enter Resources for Each Player</h1>
<% playerNames.forEach((name, index) => { %>
    <h2><%= name %></h2>
    <input type="number" id="player-resource-<%= index %>" placeholder="Resource Count" />
<% }); %>
<button onclick="handlePlayerResources()">Calculate Scores</button>