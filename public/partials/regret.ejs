<div id="page-title"><img src="images/regret.png" alt="Regret Icon"/><h2>Regret</h2><img src="images/regret.png" alt="Regret Icon"/></div>
<% for(let i = 0; i < playerData.length; ++i) { %>
    <div id="hwrapper">
        <input type="text" id="player-name-<%= i %>" value="<%= playerData[i].name %>" placeholder="Player <%= i + 1 %> Name" tabindex="<%= (i+1) %>" />
        <%= renderResourceInput(i) %>
    </div>
<% } %>
    <div id="hwrapper">
        <button onclick="setNames(); setRegret(); prev();">Back</button>
        <button onclick="setNames(); setRegret(); next();">Next</button>
    </div>