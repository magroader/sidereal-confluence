<div id="page-title"><img src="images/ship.png" alt="Ship Icon"/><h2>Ships</h2><img src="images/ship.png" alt="Ship Icon"/></div>
<% for(let i = 0; i < playerData.length; ++i) { %>
    <div id="hwrapper">
        <input type="text" id="player-name-<%= i %>" value="<%= playerData[i].name %>" placeholder="Player <%= i + 1 %> Name" tabindex="<%= (i+1) %>" />
        <%= renderResourceInput(i) %>
    </div>
<% } %>
    <div id="hwrapper">
        <button onclick="setNames(); setShips(); prev();">Back</button>
        <button onclick="setNames(); setShips(); next();">Next</button>
    </div>