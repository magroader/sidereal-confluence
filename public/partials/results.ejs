<h1>Results</h1>
<% playerData.sort((a, b) => b.score - a.score).forEach((player, index) => { %>
    <p><%= index + 1 %>. <%= player.name %>: <%= player.score %></p>
<% }); %>
<button onclick="reset()">Start Over</button>